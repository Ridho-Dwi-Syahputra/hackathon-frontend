<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External Storage -->
    <external-path
        name="external_files"
        path="." />

    <!-- Internal Storage -->
    <files-path
        name="internal_files"
        path="." />

    <!-- Cache Directory -->
    <cache-path
        name="cache_files"
        path="." />

    <!-- External Cache -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Shared Storage (untuk Android 10+) -->
    <external-files-path
        name="external_app_files"
        path="." />
</paths>